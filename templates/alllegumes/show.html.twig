{% extends 'layout.html.twig' %}

{% block title %}Alllegumes{% endblock %}

{% block main %}

    <h1>{{ alllegume.nom  }}</h1>
{#    <table class="table">#}
{#        <tbody>#}
{#            <tr>#}
{#                <th>Id</th>#}
{#                <td>{{ alllegume.id }}</td>#}
{#            </tr>#}
{#            <tr>#}
{#                <th>Nom</th>#}
{#                <td>{{ alllegume.nom }}</td>#}
{#            </tr>#}
{#        </tbody>#}
{#    </table>#}
    <h3>les <strong>{{ alllegume.nom  }}</strong> sont disponibles aux mois de:</h3>
    <div id="listMois" class="listmois row">
        <ul class="col-12">
            {% for mois in alllegume.mounth %}
                <li class="col-3">
                    <a href="{{ path('mois_show', {'id': mois.id}) }}">{{mois}}</a>
                </li>
            {% endfor %}
        </ul>

    </div>
    <div>
        <h4>Presentation : {{ alllegume.nom }}</h4>
        <p>{{ alllegume.slogan }}</p>
        <h4>Subblimer notre {{ alllegume.nom }} avec nos recettes </h4>
        <h5>Recette</h5>
        <p>{{ alllegume.recettes }}</p>
    </div>
{#    <a href="{{ path('alllegumes_index') }}">test</a>#}
    <a href="{{ path('alllegumes_index') }}">Retour a la liste des legumes</a>

{#    <a href="{{ path('alllegumes_index') }}">back to list</a>#}

    <a href="{{ path('alllegumes_edit', {'id': alllegume.id}) }}">edit</a>

{#    {{ include('alllegumes/_delete_form.html.twig') }}#}
{% endblock %}
