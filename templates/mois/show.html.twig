{% extends 'layout.html.twig' %}

{% block title %}Mois{% endblock %}

{% block main %}
    <h1>Mois</h1>
    <h2>Legumes au mois de {{ moi.nom }}</h2>

    {% for legume in moi.legume %}
    <p><a href="">{{ legume }}</a></p>
    {% endfor %}
{#    <table class="table">#}
{#        <tbody>#}
{#            <tr>#}
{#                <th>Id</th>#}
{#                <td>{{ moi.id }}</td>#}
{#            </tr>#}
{#            <tr>#}
{#                <th>Nom</th>#}
{#                <td>{{ moi.nom }}</td>#}
{#            </tr>#}
{#        </tbody>#}
{#    </table>#}

    <a href="{{ path('mois_index') }}">back to list</a>

    <a href="{{ path('mois_edit', {'id': moi.id}) }}">edit</a>

    {{ include('mois/_delete_form.html.twig') }}
{% endblock %}
