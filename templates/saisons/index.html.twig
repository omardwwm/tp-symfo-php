{% extends 'base.html.twig' %}

{% block title %}Saisons index{% endblock %}

{% block body %}
    <h1>Saisons index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for saison in saisons %}
            <tr>
                <td>{{ saison.id }}</td>
                <td>{{ saison.nom }}</td>
                <td>
                    <a href="{{ path('saisons_show', {'id': saison.id}) }}">show</a>
                    <a href="{{ path('saisons_edit', {'id': saison.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('saisons_new') }}">Create new</a>
{% endblock %}
